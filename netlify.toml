# Static site: no build step. Netlify serves the repo root as-is.
# index.html, status.json and everything else are published directly.
[build]
  publish = "."
  command = ""

[[headers]]
  for = "/status.json"
  [headers.values]
    cache-control = "no-cache"
